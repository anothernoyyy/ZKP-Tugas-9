{
  "name": "zkp-auth-blockchain",
  "version": "1.0.0",
  "description": "Blockchain Register & Login dengan Zero-Knowledge Proof (Circom + SnarkJS)",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "setup": "node scripts/setup-circuit.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
